<Window x:Class="MyBudgetController.View.SignUpWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyBudgetController.View"
        xmlns:vm="clr-namespace:MyBudgetController.ViewModel"
        mc:Ignorable="d"
        DataContext="{DynamicResource vm}"
        Title="SignUp_Win" Height="500" Width="800">
    <Window.Resources>
        <vm:SignUpWinVM x:Key="vm"/>
    </Window.Resources>
    <Grid>
        <StackPanel>
            <TextBlock Text="Sign Up" HorizontalAlignment="Center"/>

            <Label Content="Username" />
            <TextBox x:Name="username_box" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock x:Name="UsernameMessage" Text="{Binding Message_username}"/>

            <Label Content="Password" />
            <PasswordBox x:Name="pwd_box"/>

            <Label Content="Repeat Password"/>
            <PasswordBox x:Name="repeatpwd_box" PasswordChanged="PasswordChanged"/>
            <TextBlock x:Name="PasswordMessage" />

            <Button Content="Sign Up" Width="100" Margin="10" Command="{Binding SignUp}"/>
        </StackPanel>
    </Grid>
</Window>
